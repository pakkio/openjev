[project]
name = "openjev"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "huggingface-hub>=1.31.0",
    "numpy>=2.5.3",
    "safetensors>=0.8.0",
    "transformers>=5.17.0",
    "uvicorn[standard]>=0.53.0",
]

# One backend extra per platform; install exactly the one you can run.
# mlx must not be a base dependency: it installs on Linux but its libmlx.so is
# absent, and transformers' _is_mlx_available() sees the metadata and then dies
# importing it -- breaking the torch backend on any non-Apple host.
[project.optional-dependencies]
mlx = ["mlx>=0.24", "mlx-lm>=0.24"]                          # Apple silicon
torch = [
    "torch==2.9.1; sys_platform == 'linux'",  # the cu128 build verified with bitsandbytes + peft
    "torch>=2.9.1; sys_platform != 'linux'",
    "accelerate>=1.0",
    "bitsandbytes>=0.50.2",
    "peft>=0.21.0",
]  # CUDA / CPU
doom = ["vizdoom>=1.2.4"]                         # demo/doom: play Doom in the terminal with the server choosing actions

[project.scripts]
openjev = "openjev.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["openjev"]

[dependency-groups]
docs = ["mkdocs>=1.6", "mkdocs-material>=9.5", "mkdocstrings[python]>=0.26"]

# On Linux, take torch from PyTorch's CUDA 12.8 index: the default PyPI wheel is
# built for CUDA 13, which fails to load (undefined NCCL symbols) on drivers
# that only support CUDA 12.x.
[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = [{ index = "pytorch-cu128", marker = "sys_platform == 'linux'" }]
